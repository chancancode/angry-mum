- content_for :hero do
  .hero-unit
    %h1 Do I know you?

= simple_form_for @user, html: { class: 'form-horizontal' } do |form|
  = render 'form', form: form
  .form-actions
    = form.button :submit, "That's who I be", class: 'btn-primary'
